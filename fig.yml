ws:
  build: .
  links: 
    - "mysql:mysql"
  ports:
    - "8080:8080"
  # volumes:
  # - /PATH_TO_GIT_REPO/settings.xml:/root/.m2/settings.xml

mysql:
  image: mysql
  ports:
    - "3306:3306"
  environment:
    MYSQL_ROOT_PASSWORD: 123